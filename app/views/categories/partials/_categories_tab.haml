.mb-3.title.fs-2
  = I18n.t(:'catalog.categories_label')
%ul.mb-5.d-flex
  -categories.each do |category|
    %li.list-group-item
      = link_to category.name, category_path(category), class: 'btn btn-outline-dark', data: {turbo: false}
